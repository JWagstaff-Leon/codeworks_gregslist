export class Address
{
    constructor( { number, street, city, state } )
    {
        this.number = number;
        this.street = street;
        this.city = city;
        this.state = state;
    }

    get Template()
    {
        reutrn `TODO Address Template`;
    }
}